<?php
# This file is managed by Puppet.
$pdo_dsn = 'mysql:host=<%= @db_host %>;dbname=<%= @db_name %>';
$db_username = '<%= @db_username %>';
$db_password = '<%= @db_password %>';
<% if @pdo_bufsize and ! @pdo_bufsize.empty? %>
$pdo_bufsize = <%= @pdo_bufsize %>;
<% end %>
$user_auth_src = '<%= @user_auth_src %>';
$require_local_account = <% if @require_local_account == true %>TRUE<% else %>FALSE<% end %>;
<% if @ldap_options and ! @ldap_options.empty? %>
$LDAP_options = array
(<%- @ldap_options.each do |o, v| -%>
	<% if v == 'NULL' or o == 'options' or o == 'use_tls' %>'<%= o %>' => <%= v %>,<% else %>'<%= o %>' => '<%= v %>',<% end %>
<%- end -%>);<% end %>
<% if @saml_options and ! @saml_options.empty? %>
$SAML_options = array
(<%- @saml_options.each do |o, v| -%>
	<% if v == 'NULL' or o == 'options' %>'<%= o %>' => <%= v %>,<% else %>'<%= o %>' => '<%= v %>',<% end %>
<%- end -%>);
<% end %>
<% if @helpdesk_banner and ! @helpdesk_banner.empty? %>$helpdesk_banner = '<%= @helpdesk_banner %>';
<% end %>
?>
